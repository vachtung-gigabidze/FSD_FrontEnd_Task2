include ../utils/mixins

block variables

doctype html
html(lang='en')
  head
    meta(charset='utf-8')
    title #{title}
    meta(name="description", content=description)
    block head

  body(class=pageClass)
    .wrapper
      #app
        include ../includes/modules/header-ui-kit
        .content-wrapper
          block content
        

    block script